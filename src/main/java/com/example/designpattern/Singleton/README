싱글턴 패턴
'Singleton' 패턴은 프로그램에서 특정 클래스의 객체가 단 하나만 존재해야할 때 필요
이 패턴을 이해하기는 우해서는 정적멤버에 대해 알고 있어야 함

'instance' vs 'static'

인스턴스 요소들은 클래스의 새로운 인스턴스, 즉 객체가 만들어질 때 마다
instance 함께 생성되어 메모리에 자리를 차지하게 됨 (instance)

하지만 정적 요소들은 만들어지는 객체의 개수와 상관없이 딱 하나만 존재 (static)

UI 제공 프로그램에서 dark 모드 여부
